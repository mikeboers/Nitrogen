
- Put in safer localization script everywhere:
	# Setup path for local evaluation.
	# When copying to another file, just change the parameter to be accurate.
	if __name__ == '__main__':
	    def __local_eval_fix(package):
	        global __package__
	        import os, sys
	        path = os.path.abspath(__file__)
	        __package__ = package
	        sys.path.insert(0, path[:path.rfind(os.path.sep + __package__.split('.')[0])])
	        __import__(__package__)
	    __local_eval_fix('nitrogen')

- Remove all traces of the nitrogen.local, nitrogen.environ, etc.

- route rebuilding sould actually work.

- get the dispatcher into nitrogen